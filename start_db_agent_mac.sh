#!/bin/bash
# ============================================
# 提示: 本地图片服务已合并进 ERP 服务
# ============================================
# 原 db_agent.py (端口 8765) 的接口已内嵌到 ERP 服务中:
#   GET  /ping      连接测试
#   GET  /file      商品图片下载
#   POST /query     只读查询
#   POST /execute   写操作
#
# 因此无需再单独启动本脚本, 也无需为 8765 单独开隧道。
#
# 桌面上件助手连接方式 (远程模式):
#   主机: 运行 ERP 服务的机器 IP 或 SakuraFrp 隧道域名
#   端口: ERP 服务端口 8000 (或隧道对外端口)
#   令牌: 系统管理页「桌面上件助手远程通道令牌」(默认 erp2024)
#
# ERP 服务启动: python -m server.app

echo "本地图片服务已合并进 ERP 服务 (端口 8000), 无需单独启动 db_agent.py"
echo "启动 ERP 服务: python -m server.app"
