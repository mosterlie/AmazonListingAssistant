# 腾讯文档定时解析系统 · 方案

> 核心采集方式：无头浏览器内存直读（`SpreadsheetApp.workbook.worksheetManager` → `cellDataGrid`）
> 调度方式：APScheduler 常驻进程
> 数据存储：按日期写入 Excel 工作簿（每文档一簿、每采集日一 sheet）
> 结果通知：SMTP 邮件推送批次摘要 + 附件

---

## 一、架构总览

```
                ┌─────────────────── 常驻 Python 进程 (~50MB) ───────────────────┐
                │                                                                │
  config.yaml ─▶│  APScheduler 调度器 ──▶ 防重入锁 ──▶ 采集任务                     │
                │                        (锁占用则跳过)      │                     │
                │                                           ▼                     │
                │                              Playwright headless Chromium        │
                │                              (临时启动，跑完 close 归还内存)        │
                │                                           │                     │
                │                                           ▼                     │
                │                              页内直读 cellDataGrid → 结果 JSON    │
                │                                           │                     │
                │                              ┌────────────┴────────────┐         │
                │                              ▼                         ▼         │
                │                     按日期写入 Excel               邮件通知          │
                │                     (每文档一簿/每日一sheet)      (摘要+附件)        │
                └────────────────────────────────────────────────────────────────┘
```

一个常驻进程负责"掐表"，浏览器按次启动销毁——平时内存只有 ~50MB，采集期间峰值 300~600MB，跑完归零。

## 二、常驻进程设计

```
main.py 启动
  ├─ 读取 config.yaml（文档列表、定时策略、邮箱、输出配置）
  ├─ 创建/检查锁文件 (logs/.run.lock)
  ├─ APScheduler BlockingScheduler 注册触发器
  └─ scheduler.start()   ← 进程挂起，到点执行 run_batch()
```

**run_batch() 单次执行流**：

```
拿锁 → 失败则本次跳过并记日志
  ↓
启动 headless Chromium（单 page 串行复用）
  ↓
for doc in 文档列表:
    page.goto(doc.url) → wait_for_function 就绪信号 (≤60s)
    → evaluate 提取全部 sheet（usedRange 边界 + 富文本拍平）
    → 按日期写入 Excel（见第四节）
    → 每 50 个文档重启浏览器进程（防泄漏）
    → 单文档失败重试 2 次 (2s/5s)，仍失败记录清单、继续下一个
  ↓
browser.close()
  ↓
发送邮件（批次摘要 + 数据附件）
  ↓
释放锁 → 输出运行报告（成功N/失败M/耗时）
```

## 三、定时策略（可配置）

| 配置项 | 取值 | 示例 |
|---|---|---|
| `schedule.mode` | `daily` / `interval` / `cron` | 三选一 |
| `schedule.value` | 时间/秒数/表达式 | `"08:00"` / `3600` / `"0 0 8 * *"` |
| `schedule.timezone` | IANA 时区 | `Asia/Shanghai` |

- 调度器与采集任务串行，**同一时刻最多 1 个浏览器实例**（防重入锁保证）
- 上轮未结束 → 本轮跳过，日志留痕
- 每轮生成批次号（`YYYYMMDD_HHMMSS`），日志/邮件/失败清单按批次关联

## 四、数据存储方案（按日期写入表）

**主存储：每文档一个 Excel 工作簿，每次采集日期一个 sheet**：

```
data/
├── 初岛发货数据.xlsx          # 每文档一个工作簿，随日期增长
│   ├── 2026-09-21            # 当日采集的全量快照
│   ├── 2026-09-22
│   └── 2026-09-23
└── 其他文档.xlsx
```

规则：

1. **每天一个 sheet**，sheet 名即日期 `2026-09-22`；同日重复跑 → 覆盖该日 sheet（保留最新）
2. 每个 sheet 内为该文档**全部 sheet 的数据快照**（多 sheet 文档纵向堆叠，首列加 `来源sheet` 标识列）
3. 历史日期 sheet 只增不改——天然形成按日期的快照历史
4. 表头为文档原始表头（富文本已拍平为纯文本）

**备份存储（可选，默认开）**：同目录 CSV 快照 `data/csv/2026-09-22/文档名_表名.csv`，便于脚本二次消费。

## 五、邮件通知方案

每批次结束后发送，基于 SMTP（QQ邮箱/163/企业邮箱均可）：

**邮件内容**：

- 主题：`[腾讯文档采集] 2026-09-22 08:00 批次：成功 2/2，耗时 45s`
- 正文（HTML 摘要表）：

| 文档 | 状态 | sheet数 | 数据行数 | 耗时 |
|---|---|---|---|---|
| 初岛发货数据 | ✅ 成功 | 2 | 2 | 12s |

- 附件：当日**汇总 Excel**（该批次所有文档数据合成一个多 sheet 工作簿）

**触发策略（可配置）**：

| `email.on` | 行为 |
|---|---|
| `always` | 每批次必发（默认） |
| `on_failure` | 仅失败/部分失败时发 |
| `never` | 不发 |

**异常告警**：批次级故障（浏览器崩溃、锁异常、写盘失败）单独发告警邮件，附错误堆栈。

## 六、配置文件（config.yaml）

```yaml
schedule:
  mode: daily            # daily | interval | cron
  value: "08:00"
  timezone: Asia/Shanghai

docs:
  - name: 初岛发货数据
    url: https://docs.qq.com/sheet/DZUhLTndIdmJyY0dH?tab=BB08J2
  # - name: 其他表
  #   url: ...

collect:
  browser_restart_every: 50    # 每 N 个文档重启浏览器
  page_timeout: 60             # 单文档加载上限(秒)
  retry: 2                     # 失败重试次数

storage:
  data_dir: data
  excel_per_doc: true          # 每文档一簿、每日一 sheet
  csv_backup: true             # 同步落 CSV 快照
  snapshot_mode: overwrite_day # 同日重跑覆盖当日 sheet

email:
  enabled: true
  on: always                   # always | on_failure | never
  smtp:
    host: smtp.qq.com
    port: 465
    ssl: true
    user: your@qq.com
    password: "SMTP授权码（非登录密码）"
  from: your@qq.com
  to: [you@qq.com]
  subject_prefix: "[腾讯文档采集]"
  attach: excel                # excel | none

log:
  dir: logs
  retain_days: 14
```

## 七、规划目录

```
解析在线文档/
├── 方案.md                 # 本文档
├── config.yaml             # 全部可调参数
├── requirements.txt        # playwright / apscheduler / openpyxl
├── main.py                 # 入口：加载配置 → 注册调度 → 常驻
├── collector.py            # Chromium 生命周期（启动/重启/关闭/page复用）
├── extractor.py            # 页内 JS：就绪等待、sheet遍历、usedRange取值、富文本拍平
├── storage.py              # 按日期写 Excel + CSV 快照 + 汇总簿
├── mailer.py               # SMTP 邮件（摘要 HTML + 附件 + 告警）
├── logs/                   # 常驻日志 + 每批次日志 + 失败清单 + 锁文件
└── data/                   # Excel（每文档一簿）+ CSV 快照
```

各模块职责：

| 模块 | 职责 |
|---|---|
| `main.py` | 常驻入口：配置加载、防重入锁、APScheduler 注册、批次编排 |
| `collector.py` | 浏览器启动/每50个重启/批次结束关闭；单 page 串行 goto |
| `extractor.py` | 就绪信号等待、遍历 `getSheetList()`、`usedRange` 边界取值、富文本拍平、超大表分块 |
| `storage.py` | openpyxl 写入：日期 sheet 覆盖逻辑、CSV 快照、邮件用汇总簿生成 |
| `mailer.py` | SMTP(SSL) 发送：批次摘要 HTML、附件、失败告警；发信失败不影响数据落盘 |

## 八、稳定性设计

1. **就绪信号驱动**：`SpreadsheetAppInitComplete === true`，不用固定 sleep
2. **失败隔离**：单文档失败重试 2 次后记录、继续；批次级故障重启浏览器续跑
3. **内存控制**：串行复用 + 每 50 文档重启进程 + 批次结束 `close()`；只回传 `usedRange` 内数据
4. **数据校验（可选）**：与上一日 sheet 对比行列数，骤降超阈值在邮件中标注 warning
5. **发信解耦**：邮件发送失败只记日志，绝不影响数据已落盘的结果

## 九、运维

- 启动：`python main.py`（前台运行，终端打印调度状态）
- 停止：`Ctrl+C`（优雅退出，正在采集的文档跑完即止）
- 手动补跑：`python main.py --once`（跳过调度立即执行一批次）
- 开机自启（可选预留）：macOS launchd plist
- 日志滚动：按天轮转，自动清理 `retain_days` 前的旧日志

## 十、已知边界与预留

1. **仅支持公开分享文档**（匿名可访问）；私有文档预留 `storage_state` 登录态扩展位
2. 官方若改动 `SpreadsheetApp` 结构，只需修改 `extractor.py` 一处
3. 超大表（>5 万行）按行分块提取，避免单次 evaluate 返回过大
4. Excel 同文档 sheet 数量随日期增长（一年 365 个），openpyxl 无性能问题；如需归档可后续加"按年分簿"
5. SMTP 授权码属敏感信息，config.yaml 不入库不入 git（.gitignore）

## 十一、依赖清单

| 依赖 | 用途 |
|---|---|
| `playwright` | 无头浏览器驱动（首次需 `playwright install chromium`） |
| `apscheduler` | 常驻定时调度 |
| `openpyxl` | Excel 按日期读写 |
| 标准库 `smtplib` / `email` | 邮件发送 |
| 标准库 `logging` / `filelock`(或自实现) | 日志与锁 |

---

**实施顺序建议**：extractor（已验证逻辑）→ collector → storage → mailer → main 调度集成 → 用本文档链接实测端到端 → 配置定时上线。
